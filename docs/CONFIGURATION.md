# 配置参数详解

本文档详细说明 `config.yaml` 中所有配置参数的作用、影响和最佳实践。

---

## 📋 目录

- [性能配置](#性能配置)
- [质量控制配置](#质量控制配置)
- [节点配置](#节点配置)
- [订阅源配置](#订阅源配置)
- [订阅转换配置](#订阅转换配置)
- [节点质量筛选配置](#节点质量筛选配置)
- [IP风险检测配置](#ip风险检测配置)

---

## 性能配置

```yaml
performance:
  max_workers: 32              # 最大并发线程数
  content_limit_mb: 3          # 下载内容大小限制（MB）
  request_timeout: 6           # 请求超时时间（秒）
```

### `max_workers` - 最大并发线程数
- **默认值**: 32
- **作用**: 控制同时处理的订阅/节点数量
- **影响**: 
  - 更大值 → 更快的处理速度，但消耗更多CPU和网络资源
  - 更小值 → 更稳定的运行，适合低配置环境
- **建议**:
  - **GitHub Actions**: 32-64（推荐32）
  - **本地高性能**: 32-64
  - **低配服务器**: 8-16

### `content_limit_mb` - 下载限制
- **默认值**: 3 MB
- **作用**: 限制单个订阅的最大下载大小
- **影响**: 防止恶意或超大订阅导致内存溢出
- **建议**: 保持 3 MB，除非订阅源确实需要更大空间

### `request_timeout` - 请求超时
- **默认值**: 6 秒
- **作用**: 单个HTTP请求的最大等待时间
- **影响**:
  - 更短 → 更快跳过慢速源，但可能漏掉有效订阅
  - 更长 → 更完整的抓取，但整体耗时增加
- **建议**:
  - **GitHub Actions**: 6-8 秒（快速模式）
  - **本地运行**: 10-15 秒（完整模式）

---

## 质量控制配置

```yaml
quality_control:
  min_nodes: 3                 # 订阅最少节点数
  enable_duplicate_check: true # 启用内容去重
  enable_quality_check: true   # 启用质量检查
```

### `min_nodes` - 最少节点数
- **默认值**: 3
- **作用**: 过滤节点数过少的低质量订阅
- **影响**: 
  - 更大值 → 更高质量，但可能过滤掉小型但优质的订阅
  - 更小值 → 更多订阅，但可能包含低质量源
- **建议**: 3-5 个节点

### `enable_duplicate_check` - 内容去重
- **默认值**: true
- **作用**: 基于节点级别的去重（不是MD5文件去重）
- **影响**: 消除重复的节点，减少冗余
- **建议**: 保持开启

### `enable_quality_check` - 质量检查
- **默认值**: true
- **作用**: 检测空订阅、垃圾内容（如"已过期"、"请购买"）
- **影响**: 提高订阅质量
- **建议**: 保持开启

---

## 节点配置

```yaml
nodes:
  protocols:                   # 支持的节点协议
    - vmess
    - ss
    - ssr
    - trojan
    - vless
    - hysteria
    - hysteria2
```

### `protocols` - 支持的协议
- **作用**: 定义系统能识别和处理的节点协议
- **影响**: 只有列表中的协议会被识别和保存
- **建议**: 保持现有列表，覆盖主流协议

---

## 订阅源配置

### Telegram 频道

```yaml
tgchannel:
  - https://t.me/WFVPN
  - https://t.me/ripaojiedian
```

- **格式**: 支持多种格式
  - 完整URL: `https://t.me/channel_name`
  - 短链: `t.me/channel_name`
  - 纯频道名: `channel_name`
  - @ 格式: `@channel_name`
- **自动转换**: 统一转为 `https://t.me/s/channel_name` 格式
- **建议**: 定期检查频道是否还在更新

### 直连订阅源

```yaml
subscribe:
  - https://raw.githubusercontent.com/user/repo/main/sub.yaml
```

- **作用**: 直接从URL获取订阅内容
- **适用场景**: GitHub仓库、CDN订阅链接
- **建议**: 优先使用稳定的GitHub源

### 网页模糊抓取

```yaml
web_pages:
  - https://merlinblog.xyz/wiki/freess.html
  - https://fanqiangdang.com/forum-48-1.html
```

- **作用**: 从普通网页中智能提取订阅链接和节点
- **适用场景**: 博客、论坛、聚合页面
- **注意**: 可能遇到反爬虫限制

---

## 订阅转换配置

```yaml
subconverter_backends:
  - api.dler.io
  - sub.xeton.dev
  - api.v1.mk
  - sub.d1.mk
```

- **作用**: 订阅转换API后端地址
- **用途**: 验证订阅有效性，转换订阅格式
- **建议**: 保留多个后端，实现负载均衡和容错

---

## 节点质量筛选配置

```yaml
quality_filter:
  max_workers: 32              # 并发测试线程数
  connect_timeout: 5           # 连接超时时间(秒)
  max_latency: 500             # 最大可接受延迟(毫秒)
  min_speed: 0                 # 最小下载速度(KB/s)，0表示不测速
  
  # 大规模节点处理配置
  max_test_nodes: 5000         # 最多测试节点数
  max_output_nodes: 150        # 输出150个精选节点
  min_guarantee: 150           # 保底150个节点
  preferred_protocols_only: false  # 测试所有协议
  smart_sampling: true         # 智能采样
```

### 基础测试参数

#### `connect_timeout` - 连接超时
- **默认值**: 5 秒
- **作用**: TCP连接测试的超时时间
- **影响**: 影响测试速度和准确性
- **建议**: 3-8 秒

#### `max_latency` - 最大延迟
- **默认值**: 500 毫秒
- **作用**: 超过此延迟的节点会被过滤
- **影响**: 
  - 更小值 → 只保留低延迟节点，但数量较少
  - 更大值 → 更多节点，但可能包含高延迟节点
- **建议**: 300-800 毫秒

### 大规模处理参数

#### `max_test_nodes` - 最多测试节点数
- **默认值**: 5000
- **作用**: 单批次最多测试的节点数量
- **影响**: 控制测试规模和时间
- **建议**: 3000-10000

#### `max_output_nodes` - 最大输出节点数
- **默认值**: 150
- **作用**: 最终输出的高质量节点数量
- **影响**: 
  - 更多 → 更多选择，但可能包含质量较低的节点
  - 更少 → 精选节点，质量更高
- **建议**: 100-200

#### `min_guarantee` - 保底节点数
- **默认值**: 150
- **作用**: 如果可用节点不足，会继续测试直到达到此数量
- **影响**: 确保最终输出足够的节点
- **建议**: 与 `max_output_nodes` 保持一致或略小

#### `preferred_protocols_only` - 仅测试首选协议
- **默认值**: false
- **作用**: 是否只测试 `preferred_protocols` 列表中的协议
- **影响**:
  - true → 更快的测试速度，但可能漏掉其他协议的优质节点
  - false → 完整测试所有协议
- **建议**: false（全面测试）

### 协议优先级

```yaml
preferred_protocols:
  - hysteria2
  - vless
  - trojan
  - vmess
  - ss
```

- **作用**: 定义协议的优先级（从高到低）
- **影响**: 
  - 在评分系统中，首选协议会获得额外加分
  - 协议本身也有基础分数（hysteria2=10, vless=8, trojan=7等）
- **建议**: 根据实际使用场景调整

### 区域限制配置

```yaml
region_limit:
  enabled: true
  policy: score              # score模式：降分不丢弃
  allowed_countries:         # 白名单
    - US  # 美国
    - JP  # 日本
    - KR  # 韩国
    - DE  # 德国
    - GB  # 英国
    - TW  # 台湾
    - SG  # 新加坡
    - FR  # 法国
    - NL  # 荷兰
  blocked_countries:         # 黑名单（直接拒绝）
    - CN  # 中国
    - IR  # 伊朗
    - RU  # 俄罗斯
    - KP  # 朝鲜
```

#### `enabled` - 启用区域限制
- **默认值**: true
- **作用**: 是否根据节点地理位置进行筛选
- **建议**: 根据需求启用

#### `policy` - 处理策略
- **可选值**:
  - `filter`: 直接过滤掉不符合的节点
  - `score`: 不符合的节点降分，但不丢弃
- **默认值**: score
- **建议**: 使用 `score` 模式，更灵活

#### `allowed_countries` - 白名单
- **作用**: 优先保留这些国家的节点
- **影响**: `score` 模式下不在白名单的节点会降分
- **建议**: 根据实际需求配置

#### `blocked_countries` - 黑名单
- **作用**: 直接拒绝这些国家的节点
- **影响**: 黑名单国家的节点会被直接过滤
- **建议**: 保留默认配置（CN, RU, IR, KP）

---

## IP风险检测配置

```yaml
ip_risk_check:
  enabled: true              # 开启高质量IP检测
  provider: ipapi            # 提供商: ipapi (免Key)
  check_top_nodes: 300       # 检测Top 300节点
  ipapi_behavior:            # ipapi 策略
    exclude_hosting: true    # 排除机房IP
    exclude_proxy: false     # 允许代理IP
    exclude_mobile: false    # 允许移动网络
  max_risk_score: 50         # (仅 AbuseIPDB 生效)
  api_key: ""                # (仅 AbuseIPDB 生效)
```

### `enabled` - 启用IP检测
- **默认值**: true
- **作用**: 是否对节点IP进行风险检测
- **影响**: 
  - 开启 → 更高质量的节点，但测试时间更长
  - 关闭 → 更快的测试速度
- **建议**: 对于高质量需求场景保持开启

### `provider` - 检测提供商
- **可选值**:
  - `ipapi`: IP-API.com（免费，无需API Key，45次/分钟限制）
  - `abuseipdb`: AbuseIPDB（需要API Key，更精准）
- **默认值**: ipapi
- **建议**: 
  - 免费使用 → `ipapi`
  - 有API Key → `abuseipdb`（更精准）

### `check_top_nodes` - 检测节点数
- **默认值**: 300
- **作用**: 只对排名前N的节点进行IP检测
- **影响**: 
  - 更多 → 更全面的检测，但耗时更长
  - 更少 → 更快的速度，但覆盖率降低
- **建议**: 
  - ipapi（免Key）: 100-300（避免触发速率限制）
  - abuseipdb（有Key）: 300-500

### `ipapi_behavior` - IP-API 行为策略

> **重要说明**: 这些选项决定了如何判断IP的"纯净度"

#### `exclude_hosting` - 排除机房IP
- **默认值**: true
- **作用**: 排除数据中心/机房IP，只保留家庭宽带ISP
- **影响**:
  - true → 只保留家庭宽带IP（更纯净，适合解锁流媒体）
  - false → 允许机房IP（节点数量更多）
- **适用场景**:
  - **流媒体解锁**: true（Netflix、Disney+等需要家庭IP）
  - **仅翻墙**: false（机房IP速度通常更快）
- **建议**: 根据主要用途选择

#### `exclude_proxy` - 排除代理IP
- **默认值**: false
- **作用**: 是否排除被标记为代理的IP
- **影响**:
  - true → 只保留"干净"的IP
  - false → 允许代理IP（节点数量更多）
- **说明**: 
  - 很多合法的代理节点会被标记为 `proxy=true`
  - 与 `exclude_hosting` 的区别：
    - `hosting`: 指IP所属类型（机房 vs 家庭）
    - `proxy`: 指IP是否被检测到有代理行为
- **建议**: false（代理节点本身就是我们需要的）

#### `exclude_mobile` - 排除移动网络
- **默认值**: false
- **作用**: 是否排除移动网络IP（4G/5G）
- **影响**:
  - true → 只保留固定宽带IP
  - false → 允许移动网络IP
- **建议**: false（移动IP通常也能正常使用）

### 配置示例

#### 场景1: 追求极致质量（解锁流媒体）
```yaml
ip_risk_check:
  enabled: true
  provider: ipapi
  check_top_nodes: 200
  ipapi_behavior:
    exclude_hosting: true    # ✅ 只要家庭宽带
    exclude_proxy: true      # ✅ 排除被标记的代理
    exclude_mobile: true     # ✅ 排除移动网络
```

#### 场景2: 平衡质量与数量（推荐）
```yaml
ip_risk_check:
  enabled: true
  provider: ipapi
  check_top_nodes: 300
  ipapi_behavior:
    exclude_hosting: true    # ✅ 排除机房IP
    exclude_proxy: false     # ✅ 允许代理IP
    exclude_mobile: false    # ✅ 允许移动网络
```

#### 场景3: 最大节点数量
```yaml
ip_risk_check:
  enabled: false             # 关闭IP检测
```

---

## 配置模板

### 快速模式（GitHub Actions）
```yaml
performance:
  max_workers: 32
  content_limit_mb: 3
  request_timeout: 6

quality_filter:
  max_workers: 32
  connect_timeout: 5
  max_latency: 500
  max_test_nodes: 3000
  max_output_nodes: 150

ip_risk_check:
  enabled: true
  check_top_nodes: 150
```

### 完整模式（本地运行）
```yaml
performance:
  max_workers: 64
  content_limit_mb: 5
  request_timeout: 15

quality_filter:
  max_workers: 64
  connect_timeout: 8
  max_latency: 800
  max_test_nodes: 10000
  max_output_nodes: 300

ip_risk_check:
  enabled: true
  check_top_nodes: 500
```

---

## 常见问题

### Q1: 为什么节点数量很少？
**可能原因**:
1. `max_output_nodes` 设置过小
2. `max_latency` 设置过严格
3. `exclude_hosting: true` 过滤掉了大部分节点
4. 订阅源质量下降

**解决方案**:
```yaml
# 放宽限制
quality_filter:
  max_latency: 800           # 提高到800ms
  max_output_nodes: 300      # 增加输出数量

ip_risk_check:
  enabled: false             # 暂时关闭IP检测
  # 或
  ipapi_behavior:
    exclude_hosting: false   # 允许机房IP
```

### Q2: 测试时间太长
**解决方案**:
```yaml
quality_filter:
  max_workers: 64            # 增加并发
  connect_timeout: 3         # 减少超时
  max_test_nodes: 3000       # 减少测试数量

ip_risk_check:
  check_top_nodes: 100       # 减少IP检测数量
```

### Q3: 想要更高质量的节点
**解决方案**:
```yaml
quality_filter:
  max_latency: 300           # 降低延迟要求
  preferred_protocols_only: true  # 只测试首选协议

ip_risk_check:
  enabled: true
  ipapi_behavior:
    exclude_hosting: true    # 只要家庭IP
    exclude_proxy: true      # 排除代理标记
```

---

## 相关文档

- [README.md](../README.md) - 项目概览
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - 故障排查指南
- [节点格式规范](../README.md#节点格式规范) - 输出节点的命名规则
