name: Fetch Subscriptions Source

# 触发条件
on:
  workflow_dispatch:

  schedule:
    - cron: '0 20 * * *' # 北京时间凌晨4点 (UTC 20:00)
  watch:
    types: started
  # - cron: '0 3,12 * * *'
  # -'0 */1 * * *'

# 赋予 GITHUB_TOKEN 写权限
permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:

    - name: 迁出代码
      uses: actions/checkout@v4

    - name: 安装Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
      
    - name: 加载缓存
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
      
    - name: 设置时区
      run: sudo timedatectl set-timezone 'Asia/Shanghai'

    - name: 安装依赖
      run: |
        pip install -r ./requirements.txt
        
    - name: Run main.py
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY }}
        PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
      run: |
        python ./main.py || echo "⚠️ main.py 执行遇到错误，但继续执行"

    - name: Run Quality Filter & Push to Telegram
      # 只在有节点文件时才运行筛选
      if: success() || failure()  # 即使main.py失败也尝试筛选
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        ABUSEIPDB_API_KEY: ${{ secrets.ABUSEIPDB_API_KEY }}
        # 优先使用 GIST_TOKEN (需要用户创建 PAT 并赋予 gist 权限)
        # 如果不存在，尝试使用默认 GITHUB_TOKEN (通常没有 gist 权限，只能用于非 gist 操作)
        GITHUB_TOKEN: ${{ secrets.GIST_TOKEN || secrets.GITHUB_TOKEN }}
        GIST_ID: ${{ secrets.GIST_ID }}
      run: |
        # 检查是否有节点可以筛选
        if [ -f "sub/sub_all_url_check.txt" ] || [ -f "collected_nodes.txt" ]; then
          echo "✅ 发现节点文件，开始筛选..."
          python ./node_quality_filter.py || echo "⚠️ 节点筛选遇到错误"
        else
          echo "⚠️ 未发现节点文件，跳过筛选"
        fi

    - name: 拉取最新更改
      run: |
        git pull --rebase origin main || true
        
    - name: Commit
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        
        # 添加文件（分别处理，避免一个失败影响全部）
        git add ./sub || true
        git add ./collected_nodes.txt || true
        git add -f ./failed_subscriptions.log || true
        git add ./blacklist.txt || true
        
        # 检查是否有变更
        if git diff --staged --quiet; then
          echo "📝 没有文件变更，跳过提交"
        else
          git commit -m "🍀 爬取订阅源 $(date '+%Y-%m-%d %H:%M:%S')"
        fi
        
    - name: 推送更改
      # 只在有提交时才推送
      if: success()
      uses:  ad-m/github-push-action@master
      with:
         github_token: ${{ secrets.GITHUB_TOKEN }}
         branch: main
